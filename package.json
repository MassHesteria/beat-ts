{
  "name": "beat-ts",
  "private": true,
  "scripts": {
    "build": "bun build ./beat.ts --compile --outfile beats"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
